export const initialState = [
    {
        name: 'Brainey',
        age: 200,
        height: '5cm',
        id: 0
    }
];

export function smurfReducer (state = initialValue, action) {
    switch (action.type) {
        default:
            return state;
    }
}
